# test:2.2:
#   image: ruby:2.2
#   script:
#   - bundle install
#   - bundle exec rake spec

docker_image:
  image: ruby:2.2
  script:
  - apt-get update -y
  - apt-get install docker-engine -y
  - bundle install
